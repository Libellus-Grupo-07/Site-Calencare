services:
  calencare-web:
    build:
      context: ./
      image: torres5271/calencare-web
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./frontend:/home/ubuntu/frontend
    environment:
      - NODE_ENV=production
    restart: unless-stopped